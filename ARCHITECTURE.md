# ARCHITECTURE — مصاريفي

## الوضع الحالي

هذه وثيقة التصميم الأولي في مرحلة Bootstrap. لم يتم إنشاء كود Android بعد، لذلك يجب مراجعة الإصدارات والأسماء النهائية أثناء PHASE 2 قبل تثبيت المشروع.

## الاختيارات المبدئية

- المنصة: Android Native.
- اللغة: Kotlin.
- الواجهة: Jetpack Compose مع دعم RTL.
- النمط: Single Activity مع طبقات UI وDomain وData.
- التخزين: Room فوق SQLite لتخزين محلي Offline First.
- التزامن: Kotlin Coroutines وFlow.
- الحقن: Dependency Injection فقط إذا خفّض التعقيد واحتاجته البنية؛ لا تضاف مكتبة بلا سبب.
- الحد الأدنى المبدئي: Android API 26 أو أعلى، ويُراجع قبل إنشاء المشروع.
- compileSdk وtargetSdk: أحدث إصدار مستقر متاح وقت إنشاء المشروع، ويُسجل رقمه في القرار والـ README.
- العملة الافتراضية المبدئية: SAR مع قابلية تغيير القرار قبل Phase 6.

## حدود الطبقات

### UI

يعرض الحالة ويجمع إدخال المستخدم. لا يحتوي على منطق حساب مالي مستقل عن Domain.

### Domain

يحتوي على قواعد الرصيد والتدفق والديون والتحقق والتجميع. يجب أن يكون قابلًا للاختبار دون Android UI.

### Data

يتعامل مع Room وDAOs وrepositories وmigrations. لا يقرر معنى الرصيد؛ يوفّر بيانات صحيحة للـ Domain.

## مبادئ البيانات

- المبالغ تُخزن كقيمة دقيقة، مثل عدد صحيح بوحدة صغرى مناسبة للعملة، أو تمثيل عشري مضبوط؛ القرار النهائي يوثق قبل التنفيذ.
- التواريخ تُخزن بصيغة قابلة للترتيب مع timezone strategy واضحة.
- كل سجل يحتوي على createdAt وupdatedAt عند الحاجة.
- حذف السجلات المالية حساس ويجب أن يمر عبر use case واضح.
- تعديل schema يستلزم migration واختبارًا وعدم فقد البيانات.

## الكيانات المبدئية

- Transaction: دخل أو مصروف مع مبلغ وتصنيف وملاحظة وتاريخ.
- Debt: دين مستحق للمستخدم أو عليه مع المبلغ الأصلي والمتبقي والشخص والحالة.
- DebtPayment: دفعة مرتبطة بدين.
- Category: تصنيف نشط أو غير نشط.

لا تعتبر هذه البنية نهائية قبل PHASE 4.

## Offline First

تسجيل الدخل والمصروف والدين والسداد والتعديل لا يعتمد على الإنترنت. Cloud Sync والنسخ الاحتياطي ميزات لاحقة مستقلة، ولا تضاف في MVP دون قرار موثق.

## الاختبارات

- Domain: unit tests للحسابات والتحقق.
- Data: Room tests للـ DAO وmigrations.
- UI: اختبارات تدفقات الإضافة والعرض والتعديل والحذف.
- Release: smoke test وfull regression.

## الأمن

لا تُحفظ الأسرار في Git. لا تُرسل البيانات المالية لخدمة خارجية بلا سبب واضح. لا تُطبع القيم الحساسة في logs. الصلاحيات أقل ما يمكن.

## أسئلة يجب حسمها في PHASE 1 و2

- هل الحسابات متعددة مطلوبة في MVP؟ المبدئي: لا.
- هل العملة الواحدة تكفي للإصدار الأول؟ المبدئي: نعم.
- هل يلزم تسجيل دخول؟ المبدئي: لا لتطبيق محلي.
- ما سياسة النسخ الاحتياطي والاستيراد؟ تُحدد قبل Phase 11.
- ما مستوى App Lock المطلوب؟ يُدرس كميزة لاحقة ما لم يفرضه نطاق الإصدار.
